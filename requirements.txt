fastapi
uvicorn
streamlit
python-dotenv
requests
pillow
reportlab
argparse
# concurrent-futures is built-in for Python 3.5+, no need to install
# Add real Grok/xAI SDK, Printify API, Etsy SDK, etc. when integrating live keys
